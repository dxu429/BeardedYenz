<!DOCTYPE html>
<html>
  <head>
    <title>Digital Footprint</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
    <![endif]-->

    <!-- Stylesheets -->
    <link rel="stylesheet" type="text/css" href="css/bootstrap.css">
    <link rel="stylesheet" type="text/css" href="css/bootstrap-theme.css">
    <link rel="stylesheet" type="text/css" href="css/index.css">

    <!-- Google Font -->
    <link href='http://fonts.googleapis.com/css?family=Economica:400,700' rel='stylesheet' type='text/css'>

    <script src="js/wordcloud2.js" type="text/javascript"></script>
    <script src="/js/jquery-1.11.0.min.js"></script>
    <script src="/js/spin.min.js"></script>
  </head>

  <body>
    <div class="container">  
      {{#if loggedIn}}
      <div class="header2">
        <h1><img src="images/footprint.png" class="logo-small"> Welcome <span>{{me.first_name}}</span></h1>
        <hr>
        <div class="content" id="loadingMsg">
          <div class="content"><h3>You're not lost. We're tracking your footprints...</h3></div>
          <div  id="spinDiv">
          </div>
        </div>
      </div>
        <div class="row">
          <div id="canvas-container">
            <canvas id="canvas" class="canvas">
              <p>Anything in here will be replaced on browsers that support the canvas element</p>
            </canvas>
          </div>
          <div class="twittercontent">
            <img src="images/twitter.png" class="tweet"> <img src="images/twitter.png" class="tweet"> <img src="images/twitter.png" class="tweet">
            <br>
            <h2>Chirp, chirp, where are the birds calling you now?</h2>
              <hr class="short">
              {{#each timeline}}
                <h4>{{text}} <small>by <span>@{{user.screen_name}}</span></small></h4>
              {{/each}}
          </div>
        </div>
      {{else}}
      <div class="header">
        <img src="images/footprint.png" class="logo"><br>
        <h1>Welcome to <span>Digital Footprint</span>!</h1>
        <h2>Nomads of the World Wide Web, always traveling from place to place.</h2>
        <h3>Log in to view a map of where your friends have traveled.</h3>
        <div class="login">
          <a href="/login"><button>Log-in with Facebook</button></a>
        </div>
      </div>
      {{/if}}
    </div>
    <footer>
      <div class="footer"> 
        <h6>&copy; Daniel Xu 2014</h6>
      </div>
    </footer>
    <script src="js/index.js"></script>
  </body>

</html>
